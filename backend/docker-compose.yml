version: '3.9'

services:
  backend:
    image: streaming-backend
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data/

volumes:
  postgres_data:
  media_volume: